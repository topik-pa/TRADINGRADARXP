if breadcrumbs
  div(id="breadcrumbs")= __('breadcrumbs.text')
    nav
      a(href="/", title="Home")= __('header.home')
      span &nbsp;
      each elem in breadcrumbs
        span &nbsp;&#10142;&nbsp;
        a(href=elem.url, title=__(elem.name))= __(elem.name)
